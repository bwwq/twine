# 幽暗森林

四周弥漫着刺骨的雾气，树木像鬼怪一般扭曲。时不时传来野兽的嘶吼。

[[继续深入探索->野外/幽暗森林|{ action: "explore" }]]<br>
[[返回小镇广场->小镇/广场]]

{% py:
import random

if "action" in p and p["action"] == "explore":
    roll = random.randint(1, 100)
    if roll <= 20:
        # 20% 概率找到金币
        gold_found = random.randint(5, 20)
        g["gold"] += gold_found
        print(f'<div class="system-msg">你在树丛里发现了一个旧钱袋，获得了 <span class="gold">{gold_found}</span> 金币！</div>')
    else:
        # 80% 遭遇战斗
        # 随机生成怪物属性
        is_hard = random.randint(1, 10) > 7
        if is_hard:
            monster = {"name": "狂暴哥布林", "hp": 40 + g["level"] * 10, "max_hp": 40 + g["level"] * 10, "atk": 10 + g["level"] * 2, "exp": 25, "gold": 30, "is_boss": False}
        else:
            monster = {"name": "粘液史莱姆", "hp": 20 + g["level"] * 5, "max_hp": 20 + g["level"] * 5, "atk": 5 + g["level"], "exp": 10, "gold": 10, "is_boss": False}
        
        g["current_enemy"] = monster
        
        print(f'<hr><div class="danger">前方突然跳出了一只 {monster["name"]}！准备战斗！</div>')
        print('[[⚔️ 点击进入回合制战斗->战斗/进行中|{ init: true }]]')
%}