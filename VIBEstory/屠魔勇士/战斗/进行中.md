# 战斗中

{% py:
import random

# 初始化或继承战斗对象
enemy = g.get("current_enemy", None)

if not enemy:
    print('没有检测到敌人的气息。')
    print('[[返回小镇广场->小镇/广场]]')
else:
    # 战斗逻辑处理
    msg = ""
    # 若接收到玩家行动参数
    if "action" in p:
        action = p["action"]
        if action == "attack":
            dmg = g["atk"] + random.randint(-2, 2)
            if dmg < 1: dmg = 1
            enemy["hp"] -= dmg
            msg += f"你发起攻击，对 {enemy['name']} 造成了 <span class='highlight'>{dmg}</span> 点伤害！<br>"
            
        elif action == "heavy":
            if random.random() > 0.5:
                dmg = (g["atk"] * 2) + random.randint(-5, 5)
                if dmg < 2: dmg = 2
                enemy["hp"] -= dmg
                msg += f"你奋力一击！！对 {enemy['name']} 造成了 <span class='highlight'>{dmg}</span> 点巨大伤害！<br>"
            else:
                msg += "你也太用力了，破绽百出，攻击落空！<br>"

        elif action == "flee":
            if enemy.get("is_boss", False):
                msg += "在魔王的威压面前，你的双腿无法动弹，无法逃跑！<br>"
            else:
                if random.random() > 0.4:
                    print('<script>setTimeout(() => alert("成功逃离战斗！"), 50)</script>')
                    print('[[逃跑成功，点击返回->野外/幽暗森林]]')
                    enemy["hp"] = -999 # 终结状态标志
                else:
                    msg += "逃跑失败，被敌人拦住了去路！<br>"

        # 怪物反击
        if enemy["hp"] > 0 and (action != "flee" or msg.startswith("逃跑失败")):
             enemy_dmg = enemy["atk"] + random.randint(-1, 2)
             if enemy_dmg < 1: enemy_dmg = 1
             g["hp"] -= enemy_dmg
             msg += f"<span class='danger'>{enemy['name']} 发起反击，对你造成了 {enemy_dmg} 点伤害！</span><br>"

    # 死亡判断
    if g["hp"] <= 0:
        g["hp"] = 0
        enemy["hp"] = 0 
        print('<h2 class="danger">你倒下了... 世界陷入了永恒的黑暗。</h2>')
        print('[[重新开始世界线->开始]]')
    elif enemy["hp"] <= 0 and enemy.get("hp", 0) != -999: # 判定击杀
        g["current_enemy"] = None # 清除怪物
        if enemy.get("is_boss", False):
             print('<script>Freebird.goto("结局/魔王伏诛")</script>')
             print('[[继续->结局/魔王伏诛]]')
        else:
             print(f'<script>Freebird.goto("战斗/结算", {{ exp: {enemy["exp"]}, gold: {enemy["gold"]} }})</script>')
             print(f'[[战斗胜利，正在结算... (若未跳转请点击这里)->战斗/结算|{{ exp: {enemy["exp"]}, gold: {enemy["gold"]} }}]]')
    
    # 存活时渲染UI
    if g["hp"] > 0 and enemy.get("hp", 0) > 0:
        if msg:
            print(f'<div class="system-msg" style="padding:10px; background:#222; border-left:4px solid #fff;">{msg}</div>')
        
        hp_pct = (g["hp"] / g["max_hp"]) * 100
        enemy_hp_pct = (enemy["hp"] / enemy["max_hp"]) * 100
        
        print(f'''
        <div class="combat-panel">
            <div class="combat-character player-hp">
                <h3>{g["playerName"]} (Lv.{g["level"]})</h3>
                <div class="hp-bar-container"><div class="hp-bar-fill" style="width: {max(0, hp_pct)}%;"></div></div>
                <p>HP: {max(0, g["hp"])} / {g["max_hp"]}</p>
                <p>ATK: {g["atk"]}</p>
            </div>
            
            <div class="combat-character">
                <h3>{enemy["name"]}</h3>
                <div class="hp-bar-container"><div class="hp-bar-fill" style="width: {max(0, enemy_hp_pct)}%;"></div></div>
                <p>HP: {max(0, enemy["hp"])} / {enemy["max_hp"]}</p>
            </div>
        </div>
        ''')
        
        print('<h3>【行动指令】</h3>')
        print('[[⚔️ 普通攻击->战斗/进行中|{ action: "attack" }]]<br>')
        print('[[💥 舍身重击 (50%命中 / 两倍伤害)->战斗/进行中|{ action: "heavy" }]]<br>')
        if not enemy.get("is_boss", False):
            print('[[🏃 尝试逃跑 (约60%几率)->战斗/进行中|{ action: "flee" }]]')
%}