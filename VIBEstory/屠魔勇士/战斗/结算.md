# 战斗胜利！

{% py:
if "exp" in p and "gold" in p:
    exp_gain = p["exp"]
    gold_gain = p["gold"]
    
    g["exp"] += exp_gain
    g["gold"] += gold_gain
    g["monster_kills"] += 1
    
    print(f'<div class="system-msg">经过一番苦战，你获得了 <span class="highlight">{exp_gain}</span> 点经验！</div>')
    print(f'<div class="system-msg">获得了 <span class="gold">{gold_gain}</span> 枚金币！</div>')
    
    # 检查升级
    if g["exp"] >= g["exp_needed"]:
        g["level"] += 1
        g["max_hp"] += 20
        g["hp"] = g["max_hp"] 
        g["atk"] += 5
        g["exp"] -= g["exp_needed"]
        g["exp_needed"] = int(g["exp_needed"] * 1.5)
        
        print(f'<hr><h3 class="highlight">✨ 突破极限！当前等级 Lv.{g["level"]} ✨</h3>')
        print(f'<p>最大 HP 提升至 {g["max_hp"]}</p>')
        print(f'<p>攻击力提升至 {g["atk"]}</p>')
        print('<p class="system-msg">活力涌动，HP 被完全大回满了！</p>')

%}

---
[[返回幽暗森林继续探索->野外/幽暗森林]]<br>
[[带着战利品返回城镇->小镇/广场]]